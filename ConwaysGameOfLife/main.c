#include <stdio.h>
#include <stdlib.h>
#include <X11/Xlib.h>
#include <graphics.h>
#include <mpi.h>

int main(int argc, char const *argv[])
{
    MPI_Init(&argc,&argv);
    int universe[100][100];
    while()
    for (int i = 0; i < 100; ++i)
    {
        for (int j = 0; j < 100; ++j)
        {   
            
        }
    }

    MPI_Finalize();
    return 0;
}
